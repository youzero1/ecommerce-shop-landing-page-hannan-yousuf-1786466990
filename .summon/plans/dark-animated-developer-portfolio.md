---
status: implemented
title: Dark Animated Web Developer Portfolio
---

1. Create the project foundation in `package.json`, `index.html`, `tsconfig.json`, `tsconfig.app.json`, `vite.config.ts`, and `src/vite-env.d.ts`, configuring the app, animation and icon dependencies, the `@/` source alias, and file-based routing; expected outcome: the empty project can be installed and launched as a typed single-page app with generated routing support.
2. Establish the application entry point and design system in `src/main.tsx` and `src/styles/global.css`, including the required Tailwind import, dark futuristic color tokens, typography, glow treatments, selection and scrollbar styling, smooth-scroll behavior, and reduced-motion defaults; expected outcome: every section shares a cohesive high-contrast visual foundation that remains readable and accessible.
3. Build the single-page route shell in `src/routes/__root.tsx` and `src/routes/index.tsx`, arranging the loader, fixed navigation, hero, about, skills, projects, experience, testimonials, contact, social links, and footer into one continuous experience; expected outcome: all portfolio content is reachable from one URL and navigation links scroll to the correct section.
4. Define reusable portfolio content and shared models in `src/types/portfolio.ts` and `src/lib/portfolio-data.ts`, covering profile details, navigation, skills, projects, work history, testimonials, and social destinations with polished placeholder content that is easy to replace; expected outcome: visible portfolio details are managed from one structured source instead of being duplicated throughout the page.
5. Add shared animation utilities in `src/hooks/use-reduced-motion.ts`, `src/hooks/use-active-section.ts`, `src/components/animations/Reveal.tsx`, `src/components/animations/StaggerText.tsx`, and `src/components/animations/Parallax.tsx`; expected outcome: scroll reveals, staggered typography, depth effects, and active-section tracking feel consistent while automatically simplifying motion for users who request it.
6. Create the opening sequence and global interaction layer in `src/components/portfolio/LoadingIntro.tsx`, `src/components/portfolio/CustomCursor.tsx`, and `src/components/portfolio/BackgroundScene.tsx`, using a short branded loading transition, pointer-responsive cursor states, and a lightweight animated depth field; expected outcome: the site makes a memorable first impression without blocking content, harming touch devices, or overwhelming reduced-motion users.
7. Build the responsive navigation in `src/components/portfolio/Navbar.tsx` with a compact developer mark, active-section indicator, anchor links, mobile menu, and prominent contact action; expected outcome: visitors can move around the long page easily on desktop, keyboard, and mobile layouts.
8. Build the hero presentation in `src/components/portfolio/HeroSection.tsx` with animated headline typography, rotating specialty text, availability status, primary project and contact actions, social links, a layered 3D-style visual, and a scroll cue; expected outcome: visitors immediately understand the developer’s role, personality, and next actions.
9. Build the profile and capability sections in `src/components/portfolio/AboutSection.tsx` and `src/components/portfolio/SkillsSection.tsx`, combining an editorial biography, animated metrics, technology groups, and interactive skill items; expected outcome: the developer’s value, working style, and technical range are easy to scan and visually engaging.
10. Build the work showcase in `src/components/portfolio/ProjectsSection.tsx` and `src/components/portfolio/ProjectCard.tsx`, including featured imagery, technology labels, project links, pointer-aware tilt, rich hover transitions, and responsive layouts; expected outcome: each project feels interactive and clearly communicates its purpose, contribution, and available live or source links.
11. Build credibility sections in `src/components/portfolio/ExperienceSection.tsx` and `src/components/portfolio/TestimonialsSection.tsx`, using an animated career timeline and a polished testimonial carousel with accessible controls; expected outcome: visitors can quickly verify professional history and client or colleague trust signals.
12. Build the conversion and closing experience in `src/components/portfolio/ContactSection.tsx`, `src/components/portfolio/SocialLinks.tsx`, and `src/components/portfolio/Footer.tsx`, including a validated contact form that opens a prefilled email draft, direct contact details, social profiles, and a back-to-top control; expected outcome: visitors have clear, functional ways to start a conversation without requiring a backend.
13. Add reusable interface primitives in `src/components/ui/Button.tsx`, `src/components/ui/SectionHeading.tsx`, and `src/components/ui/GlowCard.tsx`; expected outcome: calls to action, section introductions, focus states, and interactive surfaces remain visually consistent across the portfolio.
14. Add portfolio media and metadata in `public/images/`, `public/favicon.svg`, `public/site.webmanifest`, and `index.html`, using optimized placeholder project artwork and complete title, description, social preview, and theme metadata; expected outcome: the site loads efficiently, has a recognizable browser identity, and presents well when shared.
15. Review and refine all work across `src/routes/index.tsx`, `src/components/portfolio/`, and `src/styles/global.css` for responsive behavior, keyboard navigation, semantic landmarks, focus visibility, color contrast, touch-device fallbacks, reduced-motion behavior, and animation performance; expected outcome: the final single-page portfolio feels smooth and premium across screen sizes while remaining usable and accessible.
